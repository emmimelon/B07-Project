<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/announcementItemView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    xmlns:tools="http://schemas.android.com/tools">
    
    <ImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:id="@+id/announcement_imageview"
        android:layout_marginRight="10dp"
        android:layout_centerVertical="true"/>
    
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/announcement_title"
        android:layout_toEndOf="@+id/announcement_imageview"
        android:textColor="@color/black"
        android:textSize="20dp"
        tools:text="Announcement Title"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/announcement_date"
        android:layout_toEndOf="@+id/announcement_imageview"
        android:layout_below="@+id/announcement_title"
        android:textColor="@color/black"
        tools:text="Date"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@id/announcement_date"
        android:layout_marginTop="10dp"
        android:background="#DAD8D8"/>

</RelativeLayout>