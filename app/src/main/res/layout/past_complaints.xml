<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/studentComplaintsView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginTop="10dp"
    xmlns:tools="http://schemas.android.com/tools">

    <ImageView
        android:layout_width="48dp"
        android:layout_height="48dp"
        android:id="@+id/complaintsIcon"
        android:layout_marginRight="10dp"
        android:layout_centerVertical="true"/>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/pastTitle"
        android:layout_toEndOf="@id/complaintsIcon"
        android:textColor="@color/black"
        android:textSize="20dp"
        tools:text="Title"/>
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/pastDate"
        android:layout_toEndOf="@id/complaintsIcon"
        android:layout_below="@id/pastTitle"
        android:textColor="@color/black"
        tools:text="Date"/>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="2dp"
        android:layout_below="@id/complaintsIcon"
        android:layout_marginTop="10dp"
        android:background="#DAD8D8"/>

</RelativeLayout>